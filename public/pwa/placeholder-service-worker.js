/**
 * Placeholder intencional — não registre este ficheiro em produção até
 * a estratégia de cache (Workbox, runtime caching, etc.) ser definida.
 * Evolução futura: trocar por `sw.js` e registar a partir de um
 * `useEffect` no cliente, ou via next-pwa, conforme a necessidade.
 */
// self.addEventListener("install", () => self.skipWaiting());
// self.addEventListener("activate", () => self.clients.claim());
